# Ingestion / document loading
llama-index-core>=0.11
llama-index-readers-file>=0.2

# Vespa client (feed + deploy + query)
pyvespa>=0.50

# Embeddings + reranker (BGE-M3, BGE-reranker-v2)
FlagEmbedding>=1.2
torch>=2.1

# Generation
anthropic>=0.40

# Utilities
python-dotenv>=1.0
